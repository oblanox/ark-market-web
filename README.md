# ARK Market Web

ARK Market Web — это веб-приложение на PHP 8.3 + Bootstrap 5, предназначенное для управления внутриигровым магазином ARK Survival Evolved. Проект подключается напрямую к боевой базе данных `ArkShopPlayers` через локальную сеть.

## 🔧 Технологии

- PHP 8.3 (с поддержкой ООП, классов, типизации, enum)
- Bootstrap 5 (адаптивная вёрстка)
- Apache 2.4 + Ubuntu Server 24.04
- Xdebug 3.2 (для отладки через VS Code)
- MySQL

## 📁 Структура проекта

```
/ark-market-web
├── public/              # Внешняя директория (index.php, формы)
│   ├── pages/           # Все страницы (login, profile, cart и т.п.)
│   ├── layout.php       # Макет страницы с подключением header/footer
│   ├── header.php       # Навигационное меню (адаптивное)
│   ├── footer.php       # Футер с контактами
│   ├── dino-list.php    # Вывод карточек товаров (динозавров)
│   ├── edit-dino.php    # Форма редактирования динозавра (для админов)
│   └── ...
├── src/
│   ├── Model/           # Модели: User, Dino, Cart, Wallet
│   ├── Service/         # Вспомогательные сервисы (SessionActivity, Wallet)
│   ├── Core/            # Ядро: render.php, роутинг и базовая логика
│   ├── Config/          # Константы (Config.php)
├── config/db.php        # Подключение к БД
├── vendor/              # Composer пакеты (Dotenv и др.)
└── .env                 # Настройки подключения (локальные)
```

## 👤 Пользователи

- Игрок регистрируется по **уникальному токену**, который он получает в игре через `/regshop`
- После регистрации он получает доступ к профилю, покупкам, смене пароля и корзине
- Покупка товаров проходит через корзину
- Команда `/delivery` в игре вызывает выдачу предметов

## 🛒 Магазин

- Все товары (динозавры, предметы, услуги) классифицируются по типу
- У динозавров есть: название, shortCode, картинка, уровень, цена, пол (или кастрация), карта (Aberration), статус Enable
- Админ может редактировать любой товар через `edit-dino.php`, включая возможность **дублирования** товара

## ⚙️ Сессии и безопасность

- Все действия пользователя трекаются через `SessionActivity`
- Ограничение смены пароля: не чаще одного раза в 5 минут
- Класс `Wallet` хранит локальный кэш баланса (обновляется через метод `refresh()`)
- В корзине параметры товара сериализуются в формате: `type|name:value|...`

## 🧠 Admin режим

- Определяется по `SteamID == 101` (зашит в `Config::ADMIN_STEAM_ID`)
- `isAdmin()` реализован в модели `User`
- Админ видит **все товары**, независимо от Enable
- У админа всегда 10 000 арков, вне зависимости от базы

## 🔍 Поиск и интерфейс

- Класс `Search` фильтрует карточки прямо на клиенте (JavaScript)
- Меню и футер адаптивны, Bootstrap 5
- Счётчик баланса в хедере: выводит число рядом с иконкой

## 📦 Доставка и покупки

- Заказ создаётся в `delivery_orders`
- После покупки списываются арки из базы
- Не полученные заказы видны игроку на отдельной странице

## 📌 Прочее

- Авторизация по email + пароль
- Отладка Xdebug работает через `client_host`
- Composer используется для подключения Dotenv и автозагрузки

---

_Фронт и серверный код регулярно проходят рефакторинг, все изменения обсуждаются и тестируются на одном тестовом пользователе до финальной интеграции._

